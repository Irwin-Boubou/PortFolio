// Not currently read by any page (tags are embedded directly inside each project/blog post
// entry instead — see the `tags` field in data/projects.ts and data/blog.ts). Kept only in
// case you want a shared tag list later.
import type { RawTag } from '@/types/content';
export const tags: RawTag[] = [];
